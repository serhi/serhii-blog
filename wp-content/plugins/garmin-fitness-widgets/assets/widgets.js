/* garmin-fitness-widgets/assets/widgets.js
 * Minimal JS — no external requests, no fetch.
 * All data is pre-rendered by PHP from garmin_data.json.
 */
(function () {
  // No client-side processing needed.
  // Dates are formatted server-side in PHP.
  // This file is intentionally minimal.
})();
